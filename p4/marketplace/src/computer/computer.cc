//computer.cc
//A simple inhereted computer product class


#include <string>
#include <iostream>
#include "computer.h"

/*
Computer::Computer(std::string id,
                        ComputerType type,
                        std::string name = "empty",
                        float price = 0.0,
                        std::string maker = "empty",
                        std::string seller = "empty") : Product(id, name, price, maker, seller)
          {
            type_ = type;
          }
*/
/*
#include<string>

#include "computer.h"


Computer::Computer(std::string id,
            ComputerType type,
            std::string name = "empty",
            float price = 0.0,
            std::string maker = "empty",
            std::string seller = "empty"):Product(
            id,
            name,
            price,
            maker,
            seller
            
            ){
                type_ = type;
                
}
*/